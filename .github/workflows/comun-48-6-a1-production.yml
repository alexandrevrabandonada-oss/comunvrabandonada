name: COMUN 48.6-A1 multidomain assisted forwarding schema rollout

on:
  workflow_dispatch:
    inputs:
      expected_main_sha:
        description: Exact merged main SHA for the controlled schema rollout
        required: true
        type: string
      execution_mode:
        description: Read-only plan, exact migration promotion, or postflight
        required: true
        default: preflight
        type: choice
        options: [preflight, promote, postflight]

concurrency:
  group: comun-48-6-a1-production
  cancel-in-progress: false

permissions:
  contents: read

env:
  A1_EXECUTION_MODE: ${{ inputs.execution_mode }}

jobs:
  rollout:
    name: A1 / controlled multidomain forwarding schema rollout
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    environment: production
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.expected_main_sha }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.18.0
          cache: npm
      - uses: supabase/setup-cli@v1
        with:
          version: 2.114.0
      - name: Assert exact Production bindings
        env:
          EXPECTED_MAIN_SHA: ${{ inputs.expected_main_sha }}
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "$SUPABASE_DB_URL"
          test -z "${SUPABASE_ACCESS_TOKEN:-}"
          test -z "${SUPABASE_SERVICE_ROLE_KEY:-}"
          test "$(git rev-parse HEAD)" = "$EXPECTED_MAIN_SHA"
      - name: Execute exact preflight, optional promotion and postflight
        env:
          EXPECTED_MAIN_SHA: ${{ inputs.expected_main_sha }}
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
          GITHUB_STEP_SUMMARY: ${{ github.step_summary }}
        shell: bash
        run: bash scripts/run-48-6-a1-production.sh
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: comun-48-6-a1-production-${{ github.run_id }}
          path: .ci-artifacts/comun-48-6-a1-production/
          retention-days: 30
          if-no-files-found: warn
