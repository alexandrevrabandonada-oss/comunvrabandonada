name: COMUN Cultural Deliverability

on:
  pull_request:
    branches: [main]
    types: [opened, synchronize, reopened, ready_for_review]
    paths:
      - ".github/workflows/comun-cultural-deliverability.yml"
      - "app/comun/acervo/**"
      - "app/comun/admin/acervo/**"
      - "app/comun/radio/**"
      - "app/comun/admin/radio/**"
      - "app/api/comun/admin/archive/upload-url/route.ts"
      - "config/radio-v1-media-profile.json"
      - "lib/archive/**"
      - "lib/archive.ts"
      - "lib/radio.ts"
      - "lib/radio-audio.ts"
      - "lib/radio-media-profile*"
      - "lib/cultural-deliverability*"
      - "scripts/audit-comun-cultural-deliverability*"
      - "scripts/comun-cultural-remote-state*"
      - "scripts/repair-comun-cultural-remote-state*"
      - "scripts/radio-v1-storage-migration*"
      - "scripts/rehearse-comun-cultural-deliverability*"
      - "supabase/migrations/*radio_v1_free_storage_profile.sql"
  push:
    branches: [main]
    paths:
      - ".github/workflows/comun-cultural-deliverability.yml"
      - "app/comun/acervo/**"
      - "app/comun/admin/acervo/**"
      - "app/comun/radio/**"
      - "app/comun/admin/radio/**"
      - "app/api/comun/admin/archive/upload-url/route.ts"
      - "config/radio-v1-media-profile.json"
      - "lib/archive/**"
      - "lib/archive.ts"
      - "lib/radio.ts"
      - "lib/radio-audio.ts"
      - "lib/radio-media-profile*"
      - "lib/cultural-deliverability*"
      - "scripts/audit-comun-cultural-deliverability*"
      - "scripts/comun-cultural-remote-state*"
      - "scripts/repair-comun-cultural-remote-state*"
      - "scripts/radio-v1-storage-migration*"
      - "scripts/rehearse-comun-cultural-deliverability*"
      - "supabase/migrations/*radio_v1_free_storage_profile.sql"
  schedule:
    - cron: "41 10 * * *"
  workflow_dispatch:
    inputs:
      mode:
        description: Canal operacional separado
        required: true
        type: choice
        options:
          - preflight
          - migrate-radio-profile
          - repair
          - postflight
          - private-rehearsal
          - content-inventory
      expected_main_sha:
        description: SHA imutável da main
        required: true
        type: string
      confirmation:
        description: Confirmação exclusiva da lane mutável ou do ensaio
        required: false
        type: string
      expected_plan_hash:
        description: Hash sanitizado do plano exato de reparo
        required: false
        type: string

concurrency:
  group: comun-cultural-deliverability-${{ github.event.pull_request.number || github.ref }}-${{ inputs.mode || 'verify' }}
  cancel-in-progress: false

permissions:
  contents: read

jobs:
  verify:
    name: VERIFY / COMUN_ARCHIVE_RADIO_ART_CONTRACT
    if: github.event_name == 'pull_request'
    runs-on: ubuntu-24.04
    timeout-minutes: 35
    steps:
      - uses: actions/checkout@v4
        with:
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.18.0
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Unit, contract and workflow tests
        run: |
          npx vitest run \
            lib/cultural-deliverability.test.ts \
            lib/archive/territorial-art.test.ts \
            lib/radio.test.ts \
            lib/radio-media-profile.test.ts
          node --test \
            scripts/audit-comun-cultural-deliverability.node-test.mjs \
            scripts/comun-cultural-remote-state.node-test.mjs \
            scripts/repair-comun-cultural-remote-state.node-test.mjs \
            scripts/radio-v1-storage-migration.node-test.mjs \
            scripts/rehearse-comun-cultural-deliverability.node-test.mjs \
            scripts/comun-cultural-deliverability-workflow.node-test.mjs
      - name: Existing cultural regressions
        run: |
          npx vitest run \
            lib/archive.test.ts \
            lib/archive/artwork-storage.test.ts \
            lib/archive/territorial-art.test.ts \
            lib/radio.test.ts
      - run: npm run typecheck
      - run: npm run lint
      - run: npm run build
      - name: Formatting and repository integrity
        run: |
          npx prettier --check \
            .github/workflows/comun-cultural-deliverability.yml \
            app/comun/acervo/arte/actions.ts \
            app/comun/admin/acervo/arte/[id]/page.tsx \
            app/comun/radio/actions.ts \
            lib/archive/territorial-art.ts \
            lib/archive/territorial-art-rules.ts \
            lib/cultural-deliverability.ts \
            lib/cultural-deliverability.test.ts \
            lib/radio.ts \
            lib/radio.test.ts \
            lib/radio-audio.ts \
            lib/radio-media-profile.mjs \
            lib/radio-media-profile.d.mts \
            lib/radio-media-profile.test.ts \
            scripts/audit-comun-cultural-deliverability.mjs \
            scripts/audit-comun-cultural-deliverability.node-test.mjs \
            scripts/comun-cultural-remote-state.mjs \
            scripts/comun-cultural-remote-state.node-test.mjs \
            scripts/repair-comun-cultural-remote-state.mjs \
            scripts/repair-comun-cultural-remote-state.node-test.mjs \
            scripts/radio-v1-storage-migration.mjs \
            scripts/radio-v1-storage-migration.node-test.mjs \
            scripts/rehearse-comun-cultural-deliverability.mjs \
            scripts/rehearse-comun-cultural-deliverability.node-test.mjs \
            scripts/comun-cultural-deliverability-workflow.node-test.mjs
          git diff --check origin/main...HEAD
      - name: Assert local and read-only PR lane
        shell: bash
        run: |
          set -euo pipefail
          ! sed -n '/^  verify:/,/^  [a-z]/p' \
            .github/workflows/comun-cultural-deliverability.yml \
            | grep -E 'secrets[.]'
          grep -q 'COMUN_ARCHIVE_RADIO_ART_CONTRACT' \
            .github/workflows/comun-cultural-deliverability.yml
          echo COMUN_ARCHIVE_RADIO_ART_CONTRACT_GREEN >> "$GITHUB_STEP_SUMMARY"

  remote-audit:
    name: AUDIT / COMUN_ARCHIVE_RADIO_ART_READ_ONLY
    if: >-
      github.event_name == 'schedule' ||
      github.event_name == 'push' ||
      (github.event_name == 'workflow_dispatch' &&
       (inputs.mode == 'preflight' ||
        inputs.mode == 'postflight' ||
        inputs.mode == 'content-inventory'))
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    outputs:
      result: ${{ steps.audit.outputs.result }}
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
      COMUN_CULTURAL_ALLOWED_PROJECT_REFS: ${{ secrets.SUPABASE_PROJECT_REF }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event_name == 'workflow_dispatch' && inputs.expected_main_sha || github.sha }}
      - name: Validate immutable main lineage
        if: github.event_name == 'workflow_dispatch'
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ inputs.expected_main_sha }}"
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          git merge-base --is-ancestor HEAD refs/remotes/origin/main
      - uses: actions/setup-node@v4
        with:
          node-version: 22.18.0
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Generate sanitized aggregate inventory
        id: audit
        shell: bash
        run: |
          set -euo pipefail
          node scripts/audit-comun-cultural-deliverability.mjs \
            --output .ci-artifacts/comun-cultural-deliverability/audit.json
          result="$(
            node -e \
              "process.stdout.write(require('./.ci-artifacts/comun-cultural-deliverability/audit.json').result)"
          )"
          echo "result=$result" >> "$GITHUB_OUTPUT"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: comun-cultural-deliverability-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/comun-cultural-deliverability/
          retention-days: 30
          if-no-files-found: error

  operational-findings:
    name: REPORT / COMUN_ARCHIVE_RADIO_ART_FINDINGS
    needs: remote-audit
    if: >-
      always() &&
      github.event_name == 'schedule' &&
      needs.remote-audit.outputs.result != ''
    runs-on: ubuntu-24.04
    permissions:
      contents: read
      issues: write
    steps:
      - uses: actions/github-script@v7
        env:
          AUDIT_RESULT: ${{ needs.remote-audit.outputs.result }}
        with:
          script: |
            const title = "[COMUN] Entregabilidade cultural — findings";
            const result = process.env.AUDIT_RESULT;
            const runUrl =
              `${context.serverUrl}/${context.repo.owner}/${context.repo.repo}` +
              `/actions/runs/${context.runId}`;
            const { data: issues } = await github.rest.issues.listForRepo({
              ...context.repo,
              state: "all",
              per_page: 100,
            });
            const existing = issues.find(
              (issue) => !issue.pull_request && issue.title === title,
            );
            const green = result === "COMUN_ARCHIVE_RADIO_ART_GREEN";
            const body = [
              `Resultado agregado sanitizado: \`${result}\`.`,
              `Run: ${runUrl}`,
              "",
              "Esta issue agrega somente estados operacionais; não contém conteúdo, IDs privados ou credenciais.",
            ].join("\n");
            if (!existing) {
              if (!green) {
                await github.rest.issues.create({
                  ...context.repo,
                  title,
                  body,
                });
              }
              return;
            }
            await github.rest.issues.createComment({
              ...context.repo,
              issue_number: existing.number,
              body,
            });
            if (green && existing.state !== "closed") {
              await github.rest.issues.update({
                ...context.repo,
                issue_number: existing.number,
                state: "closed",
              });
            } else if (!green && existing.state !== "open") {
              await github.rest.issues.update({
                ...context.repo,
                issue_number: existing.number,
                state: "open",
              });
            }

  migrate-radio-profile:
    name: MIGRATE / COMUN_RADIO_V1_FREE_STORAGE_PROFILE
    if: >-
      github.event_name == 'workflow_dispatch' &&
      inputs.mode == 'migrate-radio-profile'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    environment: production
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
      COMUN_CULTURAL_ALLOWED_PROJECT_REFS: ${{ secrets.SUPABASE_PROJECT_REF }}
      COMUN_CULTURAL_EXPECTED_PLAN_HASH: ${{ inputs.expected_plan_hash }}
      COMUN_RADIO_V1_STORAGE_MIGRATION_CONFIRMATION: ${{ inputs.confirmation }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.expected_main_sha }}
          fetch-depth: 0
      - name: Validate immutable main and exact migration envelope
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ inputs.expected_main_sha }}"
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          test "$(git rev-parse HEAD)" = "$(git rev-parse refs/remotes/origin/main)"
          test "${{ inputs.confirmation }}" = \
            "APLICAR_PERFIL_GRATUITO_RADIO_V1_47_6B"
          test "${{ inputs.expected_plan_hash }}" != ""
      - uses: actions/setup-node@v4
        with:
          node-version: 22.18.0
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Recompute read-only preflight
        run: >-
          node scripts/audit-comun-cultural-deliverability.mjs
          --output .ci-artifacts/comun-cultural-deliverability/preflight.json
      - name: Require exactly the new forward-only migration
        run: >-
          node scripts/radio-v1-storage-migration.mjs
          --audit .ci-artifacts/comun-cultural-deliverability/preflight.json
          --output .ci-artifacts/comun-cultural-deliverability/migration-plan.json
      - name: Apply only the verified profile migration
        run: >-
          node scripts/apply-radio-v1-storage-migration.mjs
          --audit .ci-artifacts/comun-cultural-deliverability/preflight.json
          --output .ci-artifacts/comun-cultural-deliverability/migration-result.json
      - name: Independent read-only bucket postflight
        run: >-
          node scripts/audit-comun-cultural-deliverability.mjs
          --output .ci-artifacts/comun-cultural-deliverability/post-migration.json
      - name: Require exact 45 MiB buckets and no objects
        shell: bash
        run: |
          set -euo pipefail
          node - <<'NODE'
          const audit = require("./.ci-artifacts/comun-cultural-deliverability/post-migration.json");
          const radio = audit.storage.buckets.filter((bucket) => bucket.id.startsWith("radio-"));
          if (
            radio.length !== 2 ||
            radio.some((bucket) => !bucket.present || !bucket.exact || bucket.fileSizeLimit !== 47185920) ||
            audit.storage.incompatibleBuckets.length !== 0
          ) process.exit(1);
          NODE
          echo COMUN_RADIO_V1_STORAGE_PROFILE_APPLIED >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: comun-radio-v1-storage-migration-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/comun-cultural-deliverability/
          retention-days: 30
          if-no-files-found: error

  repair:
    name: REPAIR / COMUN_CULTURAL_REMOTE_STATE_EXACT
    if: >-
      github.event_name == 'workflow_dispatch' &&
      inputs.mode == 'repair'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
      COMUN_CULTURAL_ALLOWED_PROJECT_REFS: ${{ secrets.SUPABASE_PROJECT_REF }}
      COMUN_CULTURAL_REPAIR_CONFIRMATION: ${{ inputs.confirmation }}
      COMUN_CULTURAL_EXPECTED_PLAN_HASH: ${{ inputs.expected_plan_hash }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.expected_main_sha }}
      - name: Validate immutable main and exact repair envelope
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ inputs.expected_main_sha }}"
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          test "$(git rev-parse HEAD)" = "$(git rev-parse refs/remotes/origin/main)"
          test "${{ inputs.confirmation }}" = \
            "EXECUTAR_CORRECAO_ALT_CULTURAL_47_6B"
          test "${{ inputs.expected_plan_hash }}" != ""
      - uses: actions/setup-node@v4
        with:
          node-version: 22.18.0
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Apply exact idempotent remote repair
        run: >-
          node scripts/repair-comun-cultural-remote-state.mjs
          --output .ci-artifacts/comun-cultural-deliverability/repair.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: comun-cultural-remote-repair-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/comun-cultural-deliverability/
          retention-days: 30
          if-no-files-found: error

  private-rehearsal:
    name: REHEARSAL / COMUN_ARCHIVE_RADIO_ART_PRIVATE
    if: >-
      github.event_name == 'workflow_dispatch' &&
      inputs.mode == 'private-rehearsal'
    runs-on: ubuntu-24.04
    timeout-minutes: 15
    env:
      SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
      SUPABASE_PROJECT_REF: ${{ secrets.SUPABASE_PROJECT_REF }}
      COMUN_CULTURAL_ALLOWED_PROJECT_REFS: ${{ secrets.SUPABASE_PROJECT_REF }}
      COMUN_CULTURAL_REHEARSAL_CONFIRMATION: ${{ inputs.confirmation }}
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ inputs.expected_main_sha }}
      - name: Validate immutable main lineage
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "${{ inputs.expected_main_sha }}"
          git fetch --no-tags origin +refs/heads/main:refs/remotes/origin/main
          git merge-base --is-ancestor HEAD refs/remotes/origin/main
          test "${{ inputs.confirmation }}" = \
            "EXECUTAR_ENSAIO_PRIVADO_ARCHIVE_RADIO_ART"
      - uses: actions/setup-node@v4
        with:
          node-version: 22.18.0
          cache: npm
      - run: npm ci --ignore-scripts
      - name: Require an independently recomputed green postflight
        shell: bash
        run: |
          set -euo pipefail
          node scripts/audit-comun-cultural-deliverability.mjs \
            --output .ci-artifacts/comun-cultural-deliverability/pre-rehearsal-audit.json
          test "$(
            node -e \
              "process.stdout.write(require('./.ci-artifacts/comun-cultural-deliverability/pre-rehearsal-audit.json').result)"
          )" = "COMUN_ARCHIVE_RADIO_ART_READY_FOR_REAL_CONTENT_REHEARSAL"
      - name: Run private transactional rehearsal
        run: >-
          node scripts/rehearse-comun-cultural-deliverability.mjs
          --output .ci-artifacts/comun-cultural-deliverability/rehearsal.json
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: comun-cultural-private-rehearsal-${{ github.sha }}-${{ github.run_id }}
          path: .ci-artifacts/comun-cultural-deliverability/
          retention-days: 30
          if-no-files-found: error
