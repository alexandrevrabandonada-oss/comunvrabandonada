name: COMUN P6C-B2 private child protection activation

on:
  workflow_dispatch:
    inputs:
      expected_main_sha:
        description: Exact merged main SHA to deploy
        required: true
        type: string
      mode:
        description: Promote migration with flags off, deploy flags off, activate private routing, or roll it back
        required: true
        type: choice
        options:
          [
            promote-migration-flags-off,
            flags-off,
            wave1-private-routing,
            rollback-private-routing,
          ]

concurrency:
  group: comun-p6c-b2-child-protection-activation
  cancel-in-progress: false
permissions:
  contents: read

env:
  EXTERNAL_MIGRATION: supabase/migrations/20260724233256_comun_sidewalk_operational_hardening.sql
  P6C_B2_MIGRATION: supabase/migrations/20260810171448_comun_child_protection_private_routing.sql
  VERCEL_ORG_ID: team_LBVwyK8FQMO7tA3hzVXXeumF
  VERCEL_PROJECT_ID: prj_BNUDaIwZKzt7IQ1PZUjo8c6Ljc3X
  COMUN_BASE_URL: https://comunsocial.online

jobs:
  activate:
    runs-on: ubuntu-24.04
    timeout-minutes: 40
    steps:
      - uses: actions/checkout@v4
        with:
          ref: main
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: 22.18.0
          cache: npm
      - uses: supabase/setup-cli@v1
        with:
          version: 2.113.0
      - name: Verify exact main and scoped activation plan
        env:
          EXPECTED_MAIN_SHA: ${{ inputs.expected_main_sha }}
        shell: bash
        run: |
          set -euo pipefail
          test "$(git rev-parse HEAD)" = "$EXPECTED_MAIN_SHA"
          npm ci --ignore-scripts
          node --check scripts/solo/rehearse-p6c-b2-child-protection-private-production.mjs
          test "$(find supabase/migrations -maxdepth 1 -type f | grep -Ei 'child_protection_private_routing' | wc -l)" -eq 1
          test -f "$P6C_B2_MIGRATION"
          ! grep -Eqi 'window\.open|wa\.me|mailto:|person_declared_sent' scripts/solo/rehearse-p6c-b2-child-protection-private-production.mjs
          ! grep -Eqi '\bdelete[[:space:]]+from\b' scripts/solo/rehearse-p6c-b2-child-protection-private-production.mjs
      - name: Promote the exact forward-only P6C-B2 migration
        if: inputs.mode == 'promote-migration-flags-off'
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "${SUPABASE_DB_URL:-}"
          case "$SUPABASE_DB_URL" in *localhost*|*127.0.0.1*) exit 1 ;; esac
          mkdir -p .ci-artifacts/p6cb2-production
          test "$(sha256sum "$EXTERNAL_MIGRATION" | awk '{print $1}')" = "6a2e69dcc66f760fa1828bb43249079e8db474ad8b175d3af6aa7c97ec05b1be"
          held="$RUNNER_TEMP/comun-p6cb2-external-ledger.sql"
          mv "$EXTERNAL_MIGRATION" "$held"
          restore() {
            test ! -e "$held" || mv "$held" "$EXTERNAL_MIGRATION"
            test "$(sha256sum "$EXTERNAL_MIGRATION" | awk '{print $1}')" = "6a2e69dcc66f760fa1828bb43249079e8db474ad8b175d3af6aa7c97ec05b1be"
          }
          trap restore EXIT
          supabase db push --db-url "$SUPABASE_DB_URL" --dry-run >.ci-artifacts/p6cb2-production/migration-dry-run.txt 2>&1
          mapfile -t planned < <(grep -oE '20[0-9]{12}_[a-z0-9_]+\.sql' .ci-artifacts/p6cb2-production/migration-dry-run.txt | sort -u || true)
          test "${#planned[@]}" -eq 1
          test "${planned[0]}" = "$(basename "$P6C_B2_MIGRATION")"
          ! grep -Eq -- '--include-all|migration repair|db reset|seed' .ci-artifacts/p6cb2-production/migration-dry-run.txt
          supabase db push --db-url "$SUPABASE_DB_URL" >.ci-artifacts/p6cb2-production/migration-push.txt 2>&1
          restore
          trap - EXIT
      - name: Postflight P6C-B2 schema without business-row reads
        if: inputs.mode == 'promote-migration-flags-off'
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
        shell: bash
        run: |
          set -euo pipefail
          psql "$SUPABASE_DB_URL" -qXAt -v ON_ERROR_STOP=1 >.ci-artifacts/p6cb2-production/schema-postflight.json <<'SQL'
          begin read only;
          select json_build_object(
            'migrationApplied', exists(select 1 from supabase_migrations.schema_migrations where version='20260810171448'),
            'childCategoryPresent', exists(select 1 from pg_catalog.pg_constraint where conrelid='public.comun_relata_cases'::regclass and conname='comun_relata_cases_category_check' and pg_catalog.pg_get_constraintdef(oid) like '%child_protection%'),
            'healthRoutingPreserved', pg_catalog.pg_get_functiondef('public.comun_relata_create(text,text,text,jsonb,text,text,text,jsonb,text,text)'::regprocedure) like '%comun-health-service-routing-v1%',
            'educationRoutingPreserved', pg_catalog.pg_get_functiondef('public.comun_relata_create(text,text,text,jsonb,text,text,text,jsonb,text,text)'::regprocedure) like '%comun-education-service-routing-v1%',
            'childRoutingAccepted', pg_catalog.pg_get_functiondef('public.comun_relata_create(text,text,text,jsonb,text,text,text,jsonb,text,text)'::regprocedure) like '%comun-child-protection-routing-v1%',
            'childPrivacyEnforced', pg_catalog.pg_get_functiondef('public.comun_relata_create(text,text,text,jsonb,text,text,text,jsonb,text,text)'::regprocedure) like '%p_privacy_class <> ''high_risk''%',
            'childPublicationNever', pg_catalog.pg_get_functiondef('public.comun_relata_create(text,text,text,jsonb,text,text,text,jsonb,text,text)'::regprocedure) like '%never_automatic%',
            'childTransitionAccepted', pg_catalog.pg_get_functiondef('public.comun_relata_classification_transition(text,text,text,text,jsonb)'::regprocedure) like '%child_protection%',
            'casesRlsForced', (select relrowsecurity and relforcerowsecurity from pg_catalog.pg_class where oid='public.comun_relata_cases'::regclass),
            'reportsRlsForced', (select relrowsecurity and relforcerowsecurity from pg_catalog.pg_class where oid='private.comun_relata_reports'::regclass),
            'classificationEventsRlsForced', (select relrowsecurity and relforcerowsecurity from pg_catalog.pg_class where oid='private.comun_relata_classification_events'::regclass),
            'anonReportsSelect', has_table_privilege('anon','private.comun_relata_reports','SELECT'),
            'authenticatedReportsSelect', has_table_privilege('authenticated','private.comun_relata_reports','SELECT'),
            'anonCreate', has_function_privilege('anon','public.comun_relata_create(text,text,text,jsonb,text,text,text,jsonb,text,text)','EXECUTE'),
            'authenticatedCreate', has_function_privilege('authenticated','public.comun_relata_create(text,text,text,jsonb,text,text,text,jsonb,text,text)','EXECUTE'),
            'serviceCreate', has_function_privilege('service_role','public.comun_relata_create(text,text,text,jsonb,text,text,text,jsonb,text,text)','EXECUTE'),
            'anonTransition', has_function_privilege('anon','public.comun_relata_classification_transition(text,text,text,text,jsonb)','EXECUTE'),
            'authenticatedTransition', has_function_privilege('authenticated','public.comun_relata_classification_transition(text,text,text,text,jsonb)','EXECUTE'),
            'serviceTransition', has_function_privilege('service_role','public.comun_relata_classification_transition(text,text,text,text,jsonb)','EXECUTE'),
            'businessRowsRead', false
          );
          rollback;
          SQL
          node - <<'NODE'
          const fs=require('node:fs');
          const s=JSON.parse(fs.readFileSync('.ci-artifacts/p6cb2-production/schema-postflight.json','utf8'));
          const required=['migrationApplied','childCategoryPresent','healthRoutingPreserved','educationRoutingPreserved','childRoutingAccepted','childPrivacyEnforced','childPublicationNever','childTransitionAccepted','casesRlsForced','reportsRlsForced','classificationEventsRlsForced','serviceCreate','serviceTransition'];
          if(required.some((key)=>s[key]!==true)||s.anonReportsSelect||s.authenticatedReportsSelect||s.anonCreate||s.authenticatedCreate||s.anonTransition||s.authenticatedTransition||s.businessRowsRead!==false) throw new Error('COMUN_P6C_B2_POSTFLIGHT_FAILED');
          NODE
          psql "$SUPABASE_DB_URL" -qX -v ON_ERROR_STOP=1 -c "notify pgrst, 'reload schema';"
      - name: Set P6C-B2 flags without reading Production values
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          test -n "${VERCEL_TOKEN:-}"
          classification=disabled
          case "${{ inputs.mode }}" in
            wave1-private-routing) classification=enabled ;;
            promote-migration-flags-off|flags-off|rollback-private-routing) ;;
            *) echo COMUN_P6C_B2_ACTIVATION_MODE_INVALID; exit 1 ;;
          esac
          printf '%s' "$classification" | npx --yes vercel@50.28.0 env add COMUN_CHILD_PROTECTION_PRIVATE_ROUTING_ENABLED production --force --yes --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-classification-env.out 2>/tmp/comun-p6cb2-classification-env.err
          printf '%s' disabled | npx --yes vercel@50.28.0 env add COMUN_SENSITIVE_FORWARDING_ASSISTED_ENABLED production --force --yes --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-forwarding-env.out 2>/tmp/comun-p6cb2-forwarding-env.err
      - name: Deploy exact main with current Production environment
        id: deploy
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          npx --yes vercel@50.28.0 deploy --prod --skip-domain --yes --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-deploy.out 2>/tmp/comun-p6cb2-deploy.err
          deployment_url="$(grep -Eo 'https://[^[:space:]]+' /tmp/comun-p6cb2-deploy.out | tail -n 1 | tr -d '\r')"
          case "$deployment_url" in https://*.vercel.app) ;; *) echo COMUN_P6C_B2_DEPLOYMENT_URL_UNAVAILABLE; exit 1 ;; esac
          printf 'deployment_url=%s\n' "$deployment_url" >> "$GITHUB_OUTPUT"
      - name: Promote exact deployment to canonical domain
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
          DEPLOYMENT_URL: ${{ steps.deploy.outputs.deployment_url }}
        shell: bash
        run: |
          set -euo pipefail
          npx --yes vercel@50.28.0 promote "$DEPLOYMENT_URL" --yes --timeout=5m --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-promote.out 2>/tmp/comun-p6cb2-promote.err
          npx --yes vercel@50.28.0 alias set "$DEPLOYMENT_URL" comunsocial.online --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-alias.out 2>/tmp/comun-p6cb2-alias.err
      - name: Verify preserved domain surfaces
        shell: bash
        run: |
          set -euo pipefail
          for path in /comun /comun/relatar /comun/calcadas /comun/onibus /comun/minha-participacao; do
            test "$(curl -L -sS -o /dev/null -w '%{http_code}' --retry 8 --retry-delay 2 "$COMUN_BASE_URL$path")" = 200
          done
      - name: Confirm flags-off deployment
        if: inputs.mode == 'promote-migration-flags-off' || inputs.mode == 'flags-off' || inputs.mode == 'rollback-private-routing'
        run: echo COMUN_P6C_B2_CODE_DEPLOYED_FLAGS_OFF >> "$GITHUB_STEP_SUMMARY"
      - name: Allocate synthetic smoke marker
        id: attempt
        if: inputs.mode == 'wave1-private-routing'
        shell: bash
        run: |
          marker="P6CB2-SMOKE-${GITHUB_RUN_ID}-$(node -e 'process.stdout.write(require("node:crypto").randomUUID())')"
          printf 'attempt_id=%s\n' "$marker" >> "$GITHUB_OUTPUT"
      - name: Run private Production smoke with finally cleanup
        id: wave1
        if: inputs.mode == 'wave1-private-routing'
        env:
          SUPABASE_DB_URL: ${{ secrets.SUPABASE_DB_URL }}
          ATTEMPT_ID: ${{ steps.attempt.outputs.attempt_id }}
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p .ci-artifacts/p6cb2-production
          node scripts/solo/rehearse-p6c-b2-child-protection-private-production.mjs >.ci-artifacts/p6cb2-production/wave1.json
          grep -q COMUN_P6C_B2_CHILD_PROTECTION_PRIVATE_WAVE1_PRODUCTION_GREEN .ci-artifacts/p6cb2-production/wave1.json
          grep -q '"externalRequests":0' .ci-artifacts/p6cb2-production/wave1.json
          grep -q '"hardDeletes":0' .ci-artifacts/p6cb2-production/wave1.json
      - name: Roll back private routing after failed gate
        if: always() && inputs.mode == 'wave1-private-routing' && steps.wave1.outcome != 'success'
        env:
          VERCEL_TOKEN: ${{ secrets.VERCEL_TOKEN }}
        shell: bash
        run: |
          set -euo pipefail
          printf '%s' disabled | npx --yes vercel@50.28.0 env add COMUN_CHILD_PROTECTION_PRIVATE_ROUTING_ENABLED production --force --yes --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-rollback-classification.out 2>/tmp/comun-p6cb2-rollback-classification.err
          printf '%s' disabled | npx --yes vercel@50.28.0 env add COMUN_SENSITIVE_FORWARDING_ASSISTED_ENABLED production --force --yes --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-rollback-forwarding.out 2>/tmp/comun-p6cb2-rollback-forwarding.err
          npx --yes vercel@50.28.0 deploy --prod --skip-domain --yes --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-rollback-deploy.out 2>/tmp/comun-p6cb2-rollback-deploy.err
          rollback_url="$(grep -Eo 'https://[^[:space:]]+' /tmp/comun-p6cb2-rollback-deploy.out | tail -n 1 | tr -d '\r')"
          case "$rollback_url" in https://*.vercel.app) ;; *) exit 1 ;; esac
          npx --yes vercel@50.28.0 promote "$rollback_url" --yes --timeout=5m --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-rollback-promote.out 2>/tmp/comun-p6cb2-rollback-promote.err
          npx --yes vercel@50.28.0 alias set "$rollback_url" comunsocial.online --token "$VERCEL_TOKEN" --scope "$VERCEL_ORG_ID" >/tmp/comun-p6cb2-rollback-alias.out 2>/tmp/comun-p6cb2-rollback-alias.err
          echo COMUN_P6C_B2_PRIVATE_ROUTING_ROLLED_BACK_AFTER_FAILED_GATE >> "$GITHUB_STEP_SUMMARY"
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: comun-p6c-b2-domain-${{ github.run_id }}
          path: .ci-artifacts/p6cb2-production
          retention-days: 14
          if-no-files-found: ignore
      - name: Emit wave marker
        if: success() && inputs.mode == 'wave1-private-routing'
        run: echo COMUN_48_1B_P6C_B2_CHILD_PROTECTION_PRIVATE_DOMAIN_GREEN_FORWARDING_OFF >> "$GITHUB_STEP_SUMMARY"
