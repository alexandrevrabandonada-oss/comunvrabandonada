# Autenticação comunitária

O COMUN usa a mesma identidade do Supabase Auth, mas separa sessão autenticada, perfil comunitário, membership e administração global. E-mail e `auth.users.id` nunca são públicos. Perfil nasce privado e membership não concede administração.
