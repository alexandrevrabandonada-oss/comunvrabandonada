import fs from "node:fs/promises";import path from "node:path";import {createClient} from "@supabase/supabase-js";import {loadLocalEnv} from "./env-loader.mjs";loadLocalEnv();
const url=process.env.NEXT_PUBLIC_SUPABASE_URL,key=process.env.SUPABASE_SERVICE_ROLE_KEY;if(!url||!key)throw new Error("Supabase server-only não configurado.");const db=createClient(url,key,{auth:{persistSession:false}});const tables=["comun_archive_items","comun_archive_collections","comun_archive_collection_items","comun_archive_relations","comun_archive_assets"];const manifest={exported_at:new Date().toISOString(),schema_version:1,storage:{provider:"r2",originals_bucket:process.env.R2_BUCKET_ORIGINALS??null,public_bucket:process.env.R2_BUCKET_PUBLIC??null},data:{}};for(const table of tables){const {data,error}=await db.from(table).select("*");if(error)throw new Error(`${table}: ${error.message}`);manifest.data[table]=data;}const day=new Date().toISOString().slice(0,10),dir=path.join(process.cwd(),"backups","acervo"),file=path.join(dir,`manifest-${day}.json`);await fs.mkdir(dir,{recursive:true});await fs.writeFile(file,JSON.stringify(manifest,null,2));console.log(`Manifest exportado: ${file}`);
