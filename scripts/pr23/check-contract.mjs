import { arg, requiredEnv } from "./lib.mjs";

const contracts = {
  backup: ["SUPABASE_ACCESS_TOKEN", "SUPABASE_PROJECT_REF", "SUPABASE_DB_URL", "SUPABASE_DB_PASSWORD", "PR23_BACKUP_BUCKET", "PR23_BACKUP_ENDPOINT", "PR23_BACKUP_ACCESS_KEY_ID", "PR23_BACKUP_SECRET_ACCESS_KEY", "PR23_BACKUP_ENCRYPTION_RECIPIENT", "PR23_BACKUP_DECRYPTION_KEY"],
  migration: ["SUPABASE_ACCESS_TOKEN", "SUPABASE_PROJECT_REF", "SUPABASE_DB_URL", "SUPABASE_DB_PASSWORD", "SUPABASE_SERVICE_ROLE_KEY"],
  domain: ["VERCEL_TOKEN", "VERCEL_TEAM_ID", "VERCEL_CANONICAL_PROJECT_ID", "VERCEL_LEGACY_PROJECT_ID"],
  merge: ["VERCEL_TOKEN", "VERCEL_TEAM_ID", "VERCEL_CANONICAL_PROJECT_ID"],
  rollback: ["SUPABASE_PROJECT_REF", "PR23_BACKUP_BUCKET", "PR23_BACKUP_ENDPOINT", "PR23_BACKUP_ACCESS_KEY_ID", "PR23_BACKUP_SECRET_ACCESS_KEY", "PR23_BACKUP_DECRYPTION_KEY", "VERCEL_TOKEN", "VERCEL_TEAM_ID", "VERCEL_CANONICAL_PROJECT_ID", "VERCEL_LEGACY_PROJECT_ID"],
};
const mode = arg("mode");
if (!contracts[mode]) throw new Error(`PR23_UNKNOWN_CONTRACT:${mode ?? "missing"}`);
requiredEnv(contracts[mode]);
console.log(`PR23_SECRET_CONTRACT_OK:${mode}`);
