export function assertLocalEnvironment(){const base=process.env.COMUN_BASE_URL||'';const supabase=process.env.NEXT_PUBLIC_SUPABASE_URL||'';const r2=process.env.R2_PUBLIC_BASE_URL||'';const deploy=`${process.env.VERCEL||''} ${process.env.VERCEL_ENV||''} ${process.env.VERCEL_URL||''}`;if(process.env.ALLOW_LOCAL_TESTS!=='true')throw new Error('ALLOW_LOCAL_TESTS=true é obrigatório.');if(!/^https?:\/\/(localhost|127\.0\.0\.1)(:\d+)?\/?$/i.test(base))throw new Error(`COMUN_BASE_URL deve ser localhost, recebido: ${base||'(ausente)'}`);if(supabase&&!/^https?:\/\/(localhost|127\.0\.0\.1)(:\d+)?/i.test(supabase))throw new Error('Supabase remoto bloqueado para smoke local.');if(/vercel\.app|supabase\.co|cloudflare|r2\.cloudflarestorage|production/i.test(`${base} ${supabase} ${r2} ${deploy}`))throw new Error('Destino ou ambiente remoto bloqueado.');console.log(`LOCAL_ENV_OK base=${base}`);return base.replace(/\/$/,'')}
