import{execFileSync,spawn}from"node:child_process";const mode=process.argv[2]||"check";process.env.DO_NOT_TRACK="1";function values(){const raw=execFileSync("powershell",["-NoProfile","-Command","$env:DO_NOT_TRACK='1'; npx supabase status -o env"],{encoding:"utf8",stdio:["ignore","pipe","inherit"]}),e=Object.fromEntries(raw.split(/\r?\n/).filter(Boolean).map(x=>{const i=x.indexOf("=");return[x.slice(0,i),x.slice(i+1).replace(/^\"|\"$/g,"")]}));if(!/^http:\/\/(localhost|127\.0\.0\.1):\d+$/.test(e.API_URL||""))throw new Error("Supabase local obrigatório.");const risky=[process.env.COMUN_BASE_URL,process.env.VERCEL_URL,process.env.R2_PUBLIC_BASE_URL].filter(Boolean).join(" ");if(/supabase\.co|vercel\.app|r2\.cloudflarestorage|cloudflare/i.test(risky))throw new Error("Destino remoto detectado no ambiente.");return{...process.env,DO_NOT_TRACK:"1",ALLOW_LOCAL_TESTS:"true",COMUN_BASE_URL:"http://localhost:3000",NEXT_PUBLIC_SITE_URL:"http://localhost:3000",NEXT_PUBLIC_SUPABASE_URL:e.API_URL,NEXT_PUBLIC_SUPABASE_ANON_KEY:e.ANON_KEY,SUPABASE_SERVICE_ROLE_KEY:e.SERVICE_ROLE_KEY,MEDIA_STORAGE_PROVIDER:"supabase-local"}}
const env=values();if(mode==="check"){console.log("COMUN_LOCAL_ENV_OK");process.exit(0)}if(mode==="print-safe"){console.log(JSON.stringify({application:env.COMUN_BASE_URL,supabase:env.NEXT_PUBLIC_SUPABASE_URL,storage_provider:env.MEDIA_STORAGE_PROVIDER,mode:"local",secrets:"redacted"},null,2));process.exit(0)}const command=process.argv[3],args=process.argv.slice(4);if(!command)throw new Error("Comando filho ausente.");const child=spawn(command,args,{env,stdio:"inherit",shell:process.platform==="win32"});child.on("exit",code=>process.exit(code??1));
