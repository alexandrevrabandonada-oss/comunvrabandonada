"use client";
import { useActionState } from "react";
import { requestCommunityPasswordReset, resetCommunityPassword } from "@/app/actions";
export function CommunityRecoveryForm() { const [state, action] = useActionState(requestCommunityPasswordReset, null); return <form action={action} className="grid gap-3"><label>E-mail<input name="email" type="email" autoComplete="email" required className="mt-1 w-full border-2 border-comun-black p-2"/></label>{state?.message ? <p role="status">{state.message}</p> : null}<button className="min-h-11 bg-comun-yellow font-black uppercase">Solicitar instruções</button></form>; }
export function CommunityResetForm() { const [state, action] = useActionState(resetCommunityPassword, null); return <form action={action} className="grid gap-3"><label>Nova senha<input name="password" type="password" minLength={10} autoComplete="new-password" required className="mt-1 w-full border-2 border-comun-black p-2"/></label><label>Confirmar nova senha<input name="password_confirmation" type="password" minLength={10} autoComplete="new-password" required className="mt-1 w-full border-2 border-comun-black p-2"/></label>{state?.error ? <p role="alert">{state.error}</p> : null}<button className="min-h-11 bg-comun-yellow font-black uppercase">Redefinir acesso</button></form>; }
