import{describe,expect,it}from"vitest";import{getOperationalSurface,OPERATIONAL_SURFACES}from"./operational-surfaces";
describe("inventário de superfícies operacionais",()=>{it("mantém chaves únicas e contratos completos",()=>{expect(OPERATIONAL_SURFACES).toHaveLength(19);expect(new Set(OPERATIONAL_SURFACES.map(x=>x.key)).size).toBe(19);for(const surface of OPERATIONAL_SURFACES){expect(surface.role).toBeTruthy();expect(surface.authorization).toBeTruthy();expect(surface.action).toBeTruthy();expect(getOperationalSurface(surface.key)).toEqual(surface)}});it("falha fechada para chave desconhecida",()=>expect(getOperationalSurface("unknown")).toBeNull())});
