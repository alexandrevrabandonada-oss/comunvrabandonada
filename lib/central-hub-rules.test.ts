import{describe,expect,it}from'vitest';import{isAchievement,isPublicAction,PAUTA_FLOW,resultLabel,sanitizeHubAuditMetadata,taskIsOverdue}from'./central-hub-rules';describe('hub central',()=>{it('mantém fluxo completo da pauta',()=>expect(PAUTA_FLOW).toContain('active_mobilization'));it('expõe somente ação aprovada',()=>{expect(isPublicAction({visibility:'public',status:'confirmed'})).toBe(true);expect(isPublicAction({visibility:'internal',status:'confirmed'})).toBe(false)});it('não trata promessa como conquista',()=>{expect(resultLabel('promise')).toBe('Promessa');expect(isAchievement({result_type:'promise',verification_status:'verified'})).toBe(false)});it('exige verificação para conquista',()=>expect(isAchievement({result_type:'achievement',verification_status:'pending'})).toBe(false));it('sanitiza auditoria',()=>expect(sanitizeHubAuditMetadata({status:'ok',private_contact:'segredo',raw_text:'segredo'})).toEqual({status:'ok'}));it('calcula tarefa atrasada sem afetar conteúdo',()=>expect(taskIsOverdue({status:'open',due_at:'2026-01-01'},new Date('2026-01-02'))).toBe(true))})
