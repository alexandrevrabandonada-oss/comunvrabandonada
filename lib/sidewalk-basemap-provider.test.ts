import {describe,expect,it} from "vitest";import {localSynthetic,realBasemapProvider,resolveSidewalkBasemapProvider} from "./sidewalk-basemap-provider";
describe("sidewalk basemap provider",()=>{it("mantém o provider remoto desligado",()=>{expect(realBasemapProvider.enabled).toBe(false);expect(resolveSidewalkBasemapProvider("realBasemapProvider")).toEqual(localSynthetic)});it("declara atribuição, cache e fallback",()=>{const provider=resolveSidewalkBasemapProvider();expect(provider.attribution).toContain("sintética");expect(provider.cachePolicy).toContain("nenhuma requisição");expect(provider.fallback).toBe("neutral-grid-and-list")})});
