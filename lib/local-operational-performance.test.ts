import{describe,expect,it}from"vitest";import{assertLocalPerformanceTarget,countOriginalAssets,percentile95,requireLocalPerformance,sanitizeLocalPerformance}from"./local-operational-performance";
describe("performance operacional local-only",()=>{it("aceita somente localhost",()=>{expect(assertLocalPerformanceTarget("http://127.0.0.1:3000").hostname).toBe("127.0.0.1");expect(()=>assertLocalPerformanceTarget("https://comun.example")).toThrow("COMUN_LOCAL_PERF_REMOTE_HOST_REFUSED")});it("exige opt-in explícito",()=>{expect(()=>requireLocalPerformance({})).toThrow("COMUN_LOCAL_PERF_DISABLED");expect(()=>requireLocalPerformance({COMUN_LOCAL_PERF:"true"})).not.toThrow()});it("remove campos sensíveis recursivamente",()=>expect(sanitizeLocalPerformance({surface:"central",cookie:"x",nested:{contact_private:"x",queries:1}})).toEqual({surface:"central",nested:{queries:1}}));it("detecta originais e calcula p95",()=>{expect(countOriginalAssets(["/thumb.webp","/private/original.jpg","/raw-upload/a.png"])).toBe(2);expect(percentile95([1,2,3,4,100])).toBe(100)})});
