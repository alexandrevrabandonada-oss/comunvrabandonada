import{describe,expect,it}from"vitest";import{validateMediaUpload}from"./r2";describe("uploads do acervo musical",()=>{it("bloqueia áudio e extensões disfarçadas",()=>{expect(()=>validateMediaUpload({scope:"private_original",key:"originals/audio.mp3",contentType:"audio/mpeg",sizeBytes:1000})).toThrow(/nao permitido/);expect(()=>validateMediaUpload({scope:"private_original",key:"originals/audio.mp3",contentType:"image/jpeg",sizeBytes:1000})).toThrow(/Extensao/)})});
