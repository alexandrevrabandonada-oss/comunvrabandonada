import{describe,expect,it}from'vitest';import{filterMapItems,isPublicTerritory,ownershipPublicWording,publicCoordinates,validateSafeGeoJson}from'./popular-map-rules';
describe('geometria territorial',()=>{it('aceita ponto válido',()=>expect(validateSafeGeoJson({type:'Point',coordinates:[-44.1,-22.5]}).ok).toBe(true));it('rejeita propriedades e coordenadas inválidas',()=>{expect(validateSafeGeoJson({type:'Point',coordinates:[999,0],properties:{private:'x'}}).ok).toBe(false)});it('oculta e aproxima coordenadas',()=>{expect(publicCoordinates({latitude:-22.123456,longitude:-44.123456,location_precision:'approximate'})).toEqual({latitude:-22.123,longitude:-44.123,precision:'approximate'});expect(publicCoordinates({latitude:-22,longitude:-44,location_precision:'hidden'})).toBeNull()})});
describe('publicação e titularidade',()=>{it('exige moderação',()=>expect(isPublicTerritory({visibility:'public',status:'active',verification_status:'source_checked'})).toBe(true));it('não afirma mais que a fonte',()=>expect(ownershipPublicWording({assertion_type:'attributed_to_company_in_public_source',attributed_party_public:'CSN',source_title:'Documento público',source_date:'2026-01-02'})).toContain('atribuída a CSN conforme Documento público'));it('filtra só campos públicos',()=>expect(filterMapItems([{name:'Ponto Centro',municipality:'Volta Redonda',neighborhood:'Centro',territory_type:'recycling_point',verification_status:'verified',layers:[{slug:'recycling'}],materials:[{slug:'paper'}]}],'centro',{front:'recycling',material:'paper'})).toHaveLength(1))});
