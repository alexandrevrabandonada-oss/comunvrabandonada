import{describe,expect,it}from"vitest";import{calculateArtistEditorialCompleteness,deriveLinkStatus,isPrivateNetworkAddress,sanitizeEditorialSnapshot,sanitizeMusicFilters}from"./music-curation-rules";describe("segurança de links",()=>{it.each(["127.0.0.1","10.1.2.3","172.16.0.1","192.168.1.1","169.254.1.1","::1","localhost"])("bloqueia %s",x=>expect(isPrivateNetworkAddress(x)).toBe(true));it("aceita IP público",()=>expect(isPrivateNetworkAddress("8.8.8.8")).toBe(false));it("marca broken após três falhas",()=>expect(deriveLinkStatus(["broken","broken","broken"])).toBe("broken"));it("rejeita redirect inseguro imediatamente",()=>expect(deriveLinkStatus(["unsafe_redirect"])).toBe("rejected"))});describe("curadoria",()=>{it("calcula completude sem ranking público",()=>{const r=calculateArtistEditorialCompleteness({stageName:"A",city:"VR",biography:"B",source:"S",members:[{}],hasImage:true,hasAltText:true,rightsReady:true,validLinks:1,releases:1});expect(r.percentage).toBe(100);expect(r.missing_requirements).toEqual([])});it("remove dados privados de snapshot",()=>expect(sanitizeEditorialSnapshot({title:"A",contact_private:"x",permission_reference_private:"y",url:"https://x.test?a=1&token=abc"})).toEqual({title:"A",url:"[redacted]"}));it("sanitiza filtros e paginação",()=>expect(sanitizeMusicFilters({q:"%,teste_",page:"-3",decade:"1990"})).toMatchObject({q:"teste",page:1,decade:1990}))});
