import{describe,it,expect}from'vitest';import{pautaAppTemplates,pautaModuleRegistry,validatePautaModuleConfig}from'./pauta-module-registry';describe('catálogo de pauta',()=>{it('é fechado e sem código configurável',()=>{expect(Object.keys(pautaModuleRegistry)).toHaveLength(18);expect(validatePautaModuleConfig('map',{html:'<script>'}).success).toBe(false)});it('templates só usam módulos permitidos',()=>{for(const modules of Object.values(pautaAppTemplates))for(const moduleType of modules)expect(pautaModuleRegistry[moduleType]).toBeDefined()})});
