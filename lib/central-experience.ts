import{performance}from"node:perf_hooks";import{createServiceSupabaseClient}from"@/lib/supabase/server";export async function getCentralExperience(){const started=performance.now(),db=createServiceSupabaseClient();if(!db)return{rounds:[],artworks:[],episodes:[],memory:[],durationMs:0};const[{data:rounds},{data:artworks},{data:episodes},{data:memory}]=await Promise.all([db.from("comun_construction_circles").select("id,title,public_question,status,pauta:comun_pauta_spaces(slug,title,visibility)").in("status",["open","synthesizing"]).limit(4),db.from("comun_archive_artworks").select("archive_item_id,title_public,description_public,territory_id,archive:comun_archive_items(slug,status,visibility),credits:comun_archive_artwork_credits(public_credit,credit_role)").eq("publication_status","published").limit(3),db.from("comun_radio_episodes").select("archive_item_id,title_public,slug_public,summary_public,published_at").eq("publication_status","published").order("published_at",{ascending:false}).limit(3),db.from("comun_archive_items").select("id,slug,title,summary,item_type,updated_at").eq("status","published").eq("visibility","public").order("updated_at",{ascending:false}).limit(4)]);return{rounds:(rounds??[]).filter((x:any)=>x.pauta?.visibility==="public"),artworks:(artworks??[]).filter((x:any)=>x.archive?.visibility==="public"),episodes:episodes??[],memory:memory??[],durationMs:Math.round(performance.now()-started)}}
export async function getPautaContinuity(pautaId:string){const db=createServiceSupabaseClient();if(!db)return[];const{data}=await db.from("comun_pauta_timeline_events").select("id,event_type,title,public_summary,occurred_at,evidence_id,action_id,protocol_id,result_id").eq("pauta_id",pautaId).eq("visibility","public").order("occurred_at",{ascending:false}).limit(20);return data??[]}
export async function getTerritoryExperience(territoryId:string){const db=createServiceSupabaseClient();if(!db)return{observatories:[],artworks:[],episodes:[],results:[],memory:[]};const[{data:observatories},{data:artworks},{data:episodes},{data:results},{data:memory}]=await Promise.all([db.from("comun_observatories").select("slug,title,public_summary,status").eq("territory_id",territoryId).eq("visibility","public").limit(4),db.from("comun_archive_artworks").select("title_public,description_public,archive:comun_archive_items!inner(slug,status,visibility)").eq("territory_id",territoryId).eq("publication_status","published").limit(4),db.from("comun_radio_episodes").select("slug_public,title_public,summary_public").eq("territory_id",territoryId).eq("publication_status","published").limit(4),db.from("comun_hub_results").select("slug,title,public_summary,result_type").eq("territory_id",territoryId).eq("visibility","public").limit(4),db.from("comun_hub_archive_links").select("relation_type,public_note,archive:comun_archive_items(slug,title,summary,status,visibility)").eq("territory_id",territoryId).limit(6)]);return{observatories:observatories??[],artworks:(artworks??[]).filter((x:any)=>x.archive?.visibility==="public"),episodes:episodes??[],results:results??[],memory:(memory??[]).filter((x:any)=>x.archive?.visibility==="public")}}
