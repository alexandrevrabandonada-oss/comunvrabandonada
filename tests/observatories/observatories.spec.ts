import AxeBuilder from'@axe-core/playwright';import{expect,test}from'@playwright/test';const routes=['/comun/observatorios','/comun/observatorios/onibus-em-movimento','/comun/observatorios/onibus-em-movimento/registrar','/comun/observatorios/onibus-em-movimento/dados','/comun/observatorios/onibus-em-movimento/mapa','/comun/observatorios/onibus-em-movimento/acoes','/comun/transporte'];for(const route of routes)test(`@visual @a11y observatório ${route}`,async({page})=>{await page.goto(route);await expect(page.locator('main')).toBeVisible();await expect(page.locator('h1')).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth+1)).toBe(false);const axe=await new AxeBuilder({page}).withTags(['wcag2a','wcag2aa','wcag21aa']).analyze();expect(axe.violations.filter(x=>['critical','serious'].includes(x.impact??''))).toEqual([])});test('@a11y formulário mantém labels e foco',async({page})=>{await page.goto('/comun/observatorios/onibus-em-movimento/registrar');await page.getByLabel('Linha').focus();await expect(page.getByLabel('Linha')).toBeFocused();await expect(page.getByRole('button',{name:'Enviar observação'})).toBeVisible()});test('@a11y admin de observatórios protegido',async({page})=>{await page.goto('/comun/admin/observatorios');await expect(page.getByRole('heading',{name:'Admin COMUN'})).toBeVisible()});
