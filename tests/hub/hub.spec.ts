import AxeBuilder from'@axe-core/playwright';import{expect,test}from'@playwright/test';const routes=['/comun','/comun/pautas','/comun/acoes','/comun/participar','/comun/projetos','/comun/projetos/vr-abandonada','/comun/territorios','/comun/territorios/volta-redonda','/comun/resultados','/comun/busca?q=Volta'];for(const route of routes)test(`@visual @a11y Hub ${route}`,async({page})=>{await page.goto(route);await expect(page.locator('main')).toBeVisible();await expect(page.locator('h1')).toBeVisible();await expect(page.getByRole('link',{name:/relatar|enviar relato/i}).first()).toBeVisible();expect(await page.evaluate(()=>document.documentElement.scrollWidth>document.documentElement.clientWidth+1)).toBe(false);const axe=await new AxeBuilder({page}).withTags(['wcag2a','wcag2aa','wcag21aa']).analyze();expect(axe.violations.filter(x=>['critical','serious'].includes(x.impact??''))).toEqual([])});test('@a11y Sala de Organização permanece protegida',async({page})=>{await page.goto('/comun/admin/organizacao');await expect(page.getByRole('heading',{name:'Admin COMUN'})).toBeVisible()})
