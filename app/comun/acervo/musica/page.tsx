import Link from"next/link";import{ComunShell,Section}from"@/components/comun-shell";import{listPublicReleases}from"@/lib/archive/local-music";export const dynamic="force-dynamic";export default async function Page(){const items=await listPublicReleases();return <ComunShell><Section><h1 className="text-4xl font-black uppercase">Música local</h1><p className="mt-3 text-comun-paper/75">Discografias documentadas. O COMUN não hospeda nem oferece download de áudio.</p><div className="mt-6 grid gap-4 md:grid-cols-3">{items.map((x:any)=><Link key={x.id} href={`/comun/acervo/musica/${x.slug}`} className="paper-panel border-2 border-comun-black p-4"><p className="text-xs font-black uppercase text-comun-rust">{x.comun_archive_music_releases?.release_type} · {x.comun_archive_music_releases?.release_year??"data desconhecida"}</p><h2 className="mt-2 text-xl font-black uppercase">{x.title}</h2><p className="mt-2">{x.summary}</p></Link>)}</div>{!items.length?<p className="mt-6">Nenhum lançamento publicado ainda.</p>:null}</Section></ComunShell>}
