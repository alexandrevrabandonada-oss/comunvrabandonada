/* eslint-disable @next/next/no-img-element -- R2 public domain varies by environment. */
import Link from "next/link";
import { ComunShell, Section } from "@/components/comun-shell";
import { archiveDate, listPublicArchiveItems } from "@/lib/archive";
export const dynamic="force-dynamic";
export default async function ArchivePage(
  props:{searchParams: Promise<{q?:string;type?:string;city?:string;neighborhood?:string;from?:string;to?:string}>}
) {
  const searchParams = await props.searchParams;
  const items=await listPublicArchiveItems(searchParams);return <ComunShell><Section><p className="font-black uppercase text-comun-yellow">Acervo vivo</p><h1 className="mt-2 text-4xl font-black uppercase text-comun-paper">Memória viva da cidade</h1><p className="mt-3 max-w-3xl text-comun-paper/75">Fotografias, documentos, lugares, artistas e coleções publicados com revisão de fonte, direitos e privacidade.</p><form className="mt-6 grid gap-2 border-2 border-comun-yellow bg-comun-black p-4 sm:grid-cols-2 lg:grid-cols-4"><input name="q" defaultValue={searchParams.q} placeholder="Buscar no acervo" className="min-h-11 bg-comun-paper px-3 text-comun-black lg:col-span-2"/><select name="type" defaultValue={searchParams.type} className="min-h-11 bg-comun-paper px-2 text-comun-black"><option value="">Todos os tipos</option>{["photograph","document","place","artist","music_release","oral_history","poster","newspaper","other"].map(x=><option key={x}>{x}</option>)}</select><input name="city" defaultValue={searchParams.city} placeholder="Cidade" className="min-h-11 bg-comun-paper px-3 text-comun-black"/><input name="neighborhood" defaultValue={searchParams.neighborhood} placeholder="Bairro" className="min-h-11 bg-comun-paper px-3 text-comun-black"/><input name="from" type="number" defaultValue={searchParams.from} placeholder="Ano inicial" className="min-h-11 bg-comun-paper px-3 text-comun-black"/><input name="to" type="number" defaultValue={searchParams.to} placeholder="Ano final" className="min-h-11 bg-comun-paper px-3 text-comun-black"/><button className="min-h-11 bg-comun-yellow px-4 font-black uppercase text-comun-black">Filtrar</button></form><div className="mt-4 flex gap-3"><Link href="/comun/acervo/colecoes" className="font-black uppercase text-comun-yellow">Ver coleções</Link><span className="text-comun-paper/50">{items.length} resultado(s)</span></div><div className="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3">{items.map(item=>{const asset=item.assets.find(a=>a.public_url);return <Link href={`/comun/acervo/${item.slug}`} key={item.id} className="paper-panel overflow-hidden border-2 border-comun-black">{asset?.public_url&&asset.mime_type?.startsWith("image/")?<img src={asset.public_url} alt={asset.alt_text??""} className="aspect-[4/3] w-full object-cover"/>:<div className="flex aspect-[4/3] items-center justify-center bg-comun-asphalt text-sm font-black uppercase text-comun-paper/60">{item.item_type}</div>}<div className="p-4"><p className="text-xs font-black uppercase text-comun-rust">{item.item_type} · {archiveDate(item)}</p><h2 className="mt-2 text-xl font-black uppercase">{item.title}</h2><p className="mt-2 text-sm text-comun-asphalt/70">{[item.place_name,item.neighborhood,item.city].filter(Boolean).join(" · ")||"Local não informado"}</p>{item.credits?<p className="mt-2 text-xs font-bold">Créditos: {item.credits}</p>:null}</div></Link>})}</div>{!items.length?<p className="mt-6 border-2 border-comun-yellow p-5 text-comun-paper/75">Nenhum item publicado corresponde aos filtros.</p>:null}</Section></ComunShell>
}
