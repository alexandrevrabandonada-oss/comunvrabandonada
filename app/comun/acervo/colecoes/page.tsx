import Link from "next/link";import {ComunShell,Section} from "@/components/comun-shell";import {listPublicCollections} from "@/lib/archive";export const dynamic="force-dynamic";export default async function Collections(){const list=await listPublicCollections();return <ComunShell><Section><Link href="/comun/acervo" className="font-black uppercase text-comun-yellow">← Acervo</Link><h1 className="mt-3 text-4xl font-black uppercase">Coleções editoriais</h1><div className="mt-6 grid gap-4 md:grid-cols-2">{list.map(c=><Link href={`/comun/acervo/colecoes/${c.slug}`} key={c.id} className="paper-panel border-2 border-comun-black p-5"><h2 className="text-xl font-black uppercase">{c.title}</h2><p className="mt-2 text-sm text-comun-asphalt/75">{c.summary}</p></Link>)}</div>{!list.length?<p className="mt-6 text-comun-paper/70">Nenhuma coleção publicada ainda.</p>:null}</Section></ComunShell>}
