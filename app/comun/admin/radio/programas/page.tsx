import Link from"next/link";import{AdminShell}from"@/components/admin-shell";import{requireComunAdmin}from"@/lib/admin-auth";import{createServiceSupabaseClient}from"@/lib/supabase/server";export default async function Page(){const s=await requireComunAdmin(),db=createServiceSupabaseClient(),{data}=db?await db.from("comun_radio_programs").select("archive_item_id,title_public,format_type,publication_status").order("created_at",{ascending:false}):{data:[]};return <AdminShell adminEmail={s.admin.email}><Link href="/comun/admin/radio/programas/novo" className="float-right border-2 border-comun-black bg-comun-yellow p-2 font-black">NOVO</Link><h1 className="text-3xl font-black uppercase">Programas</h1><div className="mt-6 grid gap-2">{data?.map(x=><div className="border-2 border-comun-black bg-white p-4" key={x.archive_item_id}><b>{x.title_public}</b> · {x.format_type} · {x.publication_status}</div>)}</div></AdminShell>}
