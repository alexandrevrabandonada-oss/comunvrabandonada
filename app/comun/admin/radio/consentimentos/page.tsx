import{AdminShell}from"@/components/admin-shell";import{requireComunAdmin}from"@/lib/admin-auth";import{createServiceSupabaseClient}from"@/lib/supabase/server";export default async function Page(){const s=await requireComunAdmin(),db=createServiceSupabaseClient(),{data}=db?await db.from("comun_radio_voice_consents").select("id,episode_item_id,consent_status,allow_comun_audio,allow_transcript,valid_until").order("created_at",{ascending:false}):{data:[]};return <AdminShell adminEmail={s.admin.email}><h1 className="text-3xl font-black uppercase">Consentimentos de voz</h1><pre className="mt-6 overflow-auto border-2 border-comun-black bg-white p-4">{JSON.stringify(data??[],null,2)}</pre></AdminShell>}
