import{AdminShell}from"@/components/admin-shell";import{requireComunAdmin}from"@/lib/admin-auth";import{createServiceSupabaseClient}from"@/lib/supabase/server";export default async function Page(){const s=await requireComunAdmin(),db=createServiceSupabaseClient(),{data}=db?await db.from("comun_radio_schedule_entries").select("id,title_public,starts_at,ends_at,schedule_type,status").order("starts_at"):{data:[]};return <AdminShell adminEmail={s.admin.email}><h1 className="text-3xl font-black uppercase">Grade editorial</h1><p className="mt-2">“Ao vivo futuro” é apenas marcador; streaming não está habilitado.</p><pre className="mt-6 overflow-auto border-2 border-comun-black bg-white p-4">{JSON.stringify(data??[],null,2)}</pre></AdminShell>}
