"use client";import{useState}from"react";export function RadioUpload({episodeId}:{episodeId:string}){const[status,setStatus]=useState(""),[assetId,setAssetId]=useState<string>();async function upload(file:File){setStatus("Validando upload privado…");const prepared=await fetch("/api/comun/admin/archive/upload-url",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({archiveItemId:episodeId,filename:file.name,mimeType:file.type,sizeBytes:file.size,role:"radio_private_original"})});const p=await prepared.json();if(!prepared.ok){setStatus(p.error);return}const sent=await fetch(p.uploadUrl,{method:"PUT",headers:{"content-type":file.type},body:file});if(!sent.ok){setStatus("Falha ao enviar original.");return}const confirmed=await fetch("/api/comun/admin/archive/confirm-upload",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({assetId:p.assetId})});if(!confirmed.ok){setStatus("Upload rejeitado na confirmação.");return}setAssetId(p.assetId);setStatus("Original privado confirmado. Aprove consentimentos e direitos antes de processar.")}async function process(){if(!assetId)return;setStatus("Processando localmente com FFmpeg…");const response=await fetch("/api/comun/admin/radio/process",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({episodeId,assetId})}),body=await response.json();setStatus(response.ok?`Derivada e waveform gerados (${body.durationSeconds}s).`:body.error)}return <div className="border-2 border-comun-black bg-white p-4"><label className="font-black uppercase" htmlFor="radio-audio">Original privado (até 250 MB)</label><input id="radio-audio" className="mt-3 block" type="file" accept="audio/wav,audio/mpeg,audio/mp4,audio/ogg,audio/flac" onChange={e=>e.target.files?.[0]&&upload(e.target.files[0])}/>{assetId?<button type="button" onClick={process} className="mt-3 border-2 border-comun-black bg-comun-yellow p-2 font-black text-comun-black">PROCESSAR APÓS APROVAÇÕES</button>:null}<p role="status" className="mt-2 text-sm">{status}</p></div>}
