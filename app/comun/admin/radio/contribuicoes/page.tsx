import{AdminShell}from"@/components/admin-shell";import{requireComunAdmin}from"@/lib/admin-auth";import{createServiceSupabaseClient}from"@/lib/supabase/server";export default async function Page(){const s=await requireComunAdmin(),db=createServiceSupabaseClient(),{data}=db?await db.from("comun_radio_contributions").select("id,public_protocol,contribution_type,title_suggestion,status,created_at").order("created_at",{ascending:false}):{data:[]};return <AdminShell adminEmail={s.admin.email}><h1 className="text-3xl font-black uppercase">Contribuições</h1><div className="mt-6 grid gap-2">{data?.map(x=><div className="border-2 border-comun-black bg-white p-4" key={x.id}><b>{x.public_protocol}</b> · {x.contribution_type} · {x.title_suggestion} · {x.status}</div>)}</div></AdminShell>}
