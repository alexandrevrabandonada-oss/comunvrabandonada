import Link from "next/link";import {AdminShell} from "@/components/admin-shell";import {requireComunAdmin} from "@/lib/admin-auth";import {createServiceSupabaseClient} from "@/lib/supabase/server";
export default async function ArtAdmin(){const session=await requireComunAdmin();const db=createServiceSupabaseClient();const{data}=db?await db.from("comun_archive_items" as never).select("id,slug,title,status,updated_at,comun_archive_artworks(artwork_type,publication_status)" as never).eq("item_type" as never,"territorial_artwork").order("updated_at" as never,{ascending:false}):{data:[]};return <AdminShell adminEmail={session.admin.email}><div className="flex justify-between gap-3"><div><p className="text-xs font-black uppercase">Acervo Vivo</p><h1 className="text-3xl font-black uppercase">Arte dos territórios</h1></div><Link href="/comun/admin/acervo/arte/novo" className="border-2 border-comun-black bg-comun-yellow px-4 py-2 font-black uppercase">Nova obra</Link></div><div className="mt-7 grid gap-3">{data?.map((x:any)=><Link key={x.id} href={`/comun/admin/acervo/arte/${x.id}`} className="border-2 border-comun-black bg-white p-4"><strong className="uppercase">{x.title}</strong><span className="ml-3 text-sm">{x.status} · {x.comun_archive_artworks?.[0]?.artwork_type}</span></Link>)}</div><nav className="mt-8 flex flex-wrap gap-3 font-bold underline"><Link href="/comun/admin/acervo/arte/contribuicoes">Contribuições</Link><Link href="/comun/admin/acervo/arte/direitos">Direitos</Link><Link href="/comun/admin/acervo/arte/creditos">Créditos</Link></nav></AdminShell>}
