import {AdminShell} from "@/components/admin-shell";import {requireComunAdmin} from "@/lib/admin-auth";import {createServiceSupabaseClient} from "@/lib/supabase/server";
export default async function ArtworkRightsQueue(){const session=await requireComunAdmin();const db=createServiceSupabaseClient();const{data}=db?await db.from("comun_archive_artwork_rights" as never).select("archive_item_id,consent_status,allow_comun_display,withdrawal_requested_at,valid_until,item:comun_archive_items(title,slug,status)" as never).order("updated_at" as never,{ascending:false}):{data:[]};return <AdminShell adminEmail={session.admin.email}><h1 className="text-3xl font-black uppercase">Direitos de arte</h1><div className="mt-6 grid gap-3">{data?.map((x:any)=><article key={x.archive_item_id} className="border-2 border-comun-black bg-white p-4"><strong>{x.item?.title}</strong><p>{x.consent_status} · exibição {x.allow_comun_display?"autorizada":"bloqueada"} · validade {x.valid_until||"sem prazo registrado"}</p></article>)}</div></AdminShell>}
