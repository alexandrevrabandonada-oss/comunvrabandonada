import Link from "next/link";import{notFound}from"next/navigation";import{AdminShell}from"@/components/admin-shell";import{requireComunAdmin}from"@/lib/admin-auth";import{resolveArtworkSubmissionReadiness}from"@/lib/archive/cultural-curation-readiness";import{createServiceSupabaseClient}from"@/lib/supabase/server";const labels:Record<string,string>={rights_review_required:"Direitos serão tratados no editor especializado depois do rascunho privado.",review_only:"A contribuição permanece em revisão privada.",authorship_unconfirmed:"O crédito sugerido não confirma autoria.",asset_not_ready:"Original privado será tratado no editor especializado.",safety_review_required:"Safety será avaliado no editor especializado.",artwork_existing_target_reconciliation_required:"Esta contribuição precisa complementar uma obra existente.",private_root_editorial_decision_required:"A decisão editorial explícita é necessária."};export const dynamic="force-dynamic";export default async function ArtworkContributionDetail({params}:{params:Promise<{id:string}>}){const session=await requireComunAdmin({roles:["admin","editor"]}),{id}=await params,db=createServiceSupabaseClient();if(!db)notFound();const{data:submission}=await db.from("comun_archive_artwork_submissions").select("id,public_protocol,submission_kind,title_suggestion,artwork_type,context_suggestion,status,creator_credit_suggestion,territory_id,authorship_source,archive_item_id,rights_state,publication_scope,reuse_permission,license_code").eq("id",id).maybeSingle();if(!submission)notFound();const readiness=resolveArtworkSubmissionReadiness(submission,{explicitEditorialDecision:false});return <AdminShell adminEmail={session.admin.email}><Link href="/comun/admin/acervo/arte/contribuicoes">← contribuições de arte</Link><h1 className="mt-4 text-3xl font-black uppercase">{submission.title_suggestion}</h1><p>{submission.public_protocol} · {submission.submission_kind} · {submission.status}</p><section className="mt-5 border-2 bg-white p-5"><h2 className="font-black uppercase">Contexto e proveniência</h2><p>{submission.context_suggestion||"Sem contexto adicional."}</p><p className="mt-2 text-sm">Crédito sugerido: {submission.creator_credit_suggestion}. Esta sugestão não cria crédito canônico nem confirma autoria.</p></section><section className="mt-5 border-2 bg-white p-5"><h2 className="font-black uppercase">Readiness</h2><p>Raiz privada: {readiness.readyForPrivateRootCreation?"pode ser criada após decisão editorial e rollout do schema":"ainda bloqueada"}.</p><ul className="mt-2 list-disc pl-5">{readiness.blockers.map(blocker=><li key={blocker}>{labels[blocker]||blocker}</li>)}</ul><p className="mt-3 font-bold">Publicação não está autorizada.</p></section>{submission.archive_item_id?<Link className="btn mt-5 inline-block" href={`/comun/admin/acervo/arte/${submission.archive_item_id}`}>Abrir rascunho privado</Link>:<section className="mt-5 border-2 border-amber-800 bg-amber-50 p-5"><h2 className="font-black uppercase">Próxima ação segura</h2><p>A primitive atômica de Arte está versionada, mas ainda não está instalada em Production. O botão “Criar rascunho privado” permanecerá indisponível até o rollout A5-A2-R1.</p></section>}</AdminShell>}
