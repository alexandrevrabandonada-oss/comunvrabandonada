import {AdminShell} from "@/components/admin-shell";import {requireComunAdmin} from "@/lib/admin-auth";import {createServiceSupabaseClient} from "@/lib/supabase/server";
export default async function ArtworkCreditsQueue(){const session=await requireComunAdmin();const db=createServiceSupabaseClient();const{data}=db?await db.from("comun_archive_artwork_credits" as never).select("id,public_credit,credit_role,public_visibility,item:comun_archive_items(title),agent:comun_archive_agents(public_name,agent_type,status)" as never).order("created_at" as never,{ascending:false}):{data:[]};return <AdminShell adminEmail={session.admin.email}><h1 className="text-3xl font-black uppercase">Créditos de arte</h1><div className="mt-6 grid gap-3">{data?.map((x:any)=><article key={x.id} className="border-2 border-comun-black bg-white p-4"><strong>{x.public_credit}</strong><p>{x.credit_role} · {x.item?.title} · {x.agent?.agent_type}</p></article>)}</div></AdminShell>}
