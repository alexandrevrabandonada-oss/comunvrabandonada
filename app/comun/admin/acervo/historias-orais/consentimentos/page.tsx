import Link from 'next/link';import{AdminShell}from'@/components/admin-shell';import{requireComunAdmin}from'@/lib/admin-auth';import{createServiceSupabaseClient}from'@/lib/supabase/server';
export const dynamic='force-dynamic';export default async function Page(){const s=await requireComunAdmin(),db=createServiceSupabaseClient();const{data}=db?await db.from('comun_archive_oral_history_consents').select('id,oral_history_item_id,consent_status,valid_until').order('updated_at',{ascending:false}):{data:[]};return <AdminShell adminEmail={s.admin.email}><h1 className="text-3xl font-black uppercase">Consentimentos de História Oral</h1><div className="mt-6 grid gap-2">{data?.map((x:any)=><Link className="border-2 bg-white p-4" key={x.id} href={`/comun/admin/acervo/historias-orais/${x.oral_history_item_id}`}>Entrevista {x.oral_history_item_id.slice(0,8)} · {x.consent_status} · {x.valid_until??'sem prazo'}</Link>)}</div></AdminShell>}
