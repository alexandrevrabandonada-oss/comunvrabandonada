import Link from 'next/link';import { AdminShell } from '@/components/admin-shell';import { requireComunAdmin } from '@/lib/admin-auth';import { createServiceSupabaseClient } from '@/lib/supabase/server';
export const dynamic='force-dynamic';export default async function Page(){const session=await requireComunAdmin(),db=createServiceSupabaseClient();const{data}=db?await db.from('comun_archive_items').select('id,title,slug,status,created_at,comun_archive_oral_histories(publication_status,transcript_status,embargo_until)').eq('item_type','oral_history').order('created_at',{ascending:false}):{data:[]};return <AdminShell adminEmail={session.admin.email}><div className="flex flex-wrap justify-between gap-3"><div><h1 className="text-3xl font-black uppercase">Histórias orais</h1><p>Originais privados, consentimento granular e publicação seletiva.</p></div><Link className="border-2 border-comun-black bg-comun-yellow px-4 py-3 font-black" href="/comun/admin/acervo/historias-orais/novo">Nova entrevista</Link></div><div className="mt-6 grid gap-3">{data?.map((item:any)=><Link key={item.id} href={`/comun/admin/acervo/historias-orais/${item.id}`} className="border-2 border-comun-black bg-white p-4"><h2 className="font-black uppercase">{item.title}</h2><p>{item.comun_archive_oral_histories?.publication_status} · transcrição {item.comun_archive_oral_histories?.transcript_status}</p></Link>)}</div></AdminShell>}
