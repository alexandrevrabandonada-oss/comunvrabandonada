import Link from 'next/link';import{AdminShell}from'@/components/admin-shell';import{requireComunAdmin}from'@/lib/admin-auth';import{createServiceSupabaseClient}from'@/lib/supabase/server';
export const dynamic='force-dynamic';export default async function Page(){const s=await requireComunAdmin(),db=createServiceSupabaseClient();const{data}=db?await db.from('comun_archive_oral_histories').select('archive_item_id,transcript_status').neq('transcript_status','approved_public').order('updated_at',{ascending:true}):{data:[]};return <AdminShell adminEmail={s.admin.email}><h1 className="text-3xl font-black uppercase">Fila de transcrições</h1><div className="mt-6 grid gap-2">{data?.map((x:any)=><Link className="border-2 bg-white p-4" key={x.archive_item_id} href={`/comun/admin/acervo/historias-orais/${x.archive_item_id}`}>Entrevista {x.archive_item_id.slice(0,8)} · {x.transcript_status}</Link>)}</div></AdminShell>}
