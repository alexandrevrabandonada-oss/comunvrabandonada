import{AdminShell}from"@/components/admin-shell";import{requireComunAdmin}from"@/lib/admin-auth";import{createServiceSupabaseClient}from"@/lib/supabase/server";import{reviewArtistSubmission}from"./actions";export const dynamic="force-dynamic";export default async function Page(){const s=await requireComunAdmin(),db=createServiceSupabaseClient(),{data}=db?await db.from("comun_archive_artist_submissions").select("id,status,stage_name,artist_type,city,genres,biography_suggested,members_suggested,releases_suggested,official_links_suggested,relationship_to_artist,is_artist_or_representative,source_reference,applied_fields,decision_note,created_at").order("created_at",{ascending:false}):{data:[]};return <AdminShell adminEmail={s.admin.email}><h1 className="text-3xl font-black uppercase">Contribuições de artistas</h1><p className="mt-2">Comparação e incorporação manual; nenhum envio altera perfil sozinho.</p><div className="mt-6 grid gap-4">{data?.map(x=><article key={x.id} className="border-2 border-comun-black bg-white p-4"><h2 className="font-black uppercase">{x.stage_name}</h2><p>{x.artist_type} · {x.city} · {x.status}</p><div className="mt-3 grid gap-3 md:grid-cols-2"><div><h3 className="font-bold">Conteúdo sugerido</h3><p>{x.biography_suggested||"Sem biografia"}</p><p>Integrantes: {x.members_suggested||"—"}</p><p>Lançamentos: {x.releases_suggested||"—"}</p></div><div><h3 className="font-bold">Fonte e contexto</h3><p>{x.source_reference||"Fonte não informada"}</p><p>{x.relationship_to_artist||"Relação não informada"}</p></div></div><form action={reviewArtistSubmission} className="mt-4 grid gap-2"><input type="hidden" name="id" value={x.id}/><fieldset><legend className="font-bold">Campos incorporados manualmente</legend>{["biografia","integrantes","lançamentos","links","território","fonte"].map(v=><label key={v} className="mr-3"><input type="checkbox" name="applied_fields" value={v} defaultChecked={x.applied_fields?.includes(v)}/> {v}</label>)}</fieldset><select name="status" defaultValue={x.status}><option value="information_requested">Pedir esclarecimento</option><option value="partially_applied">Aceitar parcialmente</option><option value="approved">Incorporado</option><option value="rejected">Rejeitar</option><option value="archived">Arquivar</option></select><input name="decision_note" defaultValue={x.decision_note??""} placeholder="Decisão editorial"/><button className="border-2 border-comun-black bg-comun-yellow p-2 font-bold">Registrar decisão</button></form></article>)}</div></AdminShell>}
