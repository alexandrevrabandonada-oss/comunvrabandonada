import{AdminShell}from"@/components/admin-shell";import{requireComunAdmin}from"@/lib/admin-auth";import{createServiceSupabaseClient}from"@/lib/supabase/server";import{reviewClaim}from"../actions";export const dynamic="force-dynamic";export default async function Page(){const s=await requireComunAdmin(),db=createServiceSupabaseClient(),{data}=db?await db.from("comun_archive_artist_claims").select("id,artist_item_id,claimant_name,claimant_contact_private,relationship,verification_reference_private,status,created_at,comun_archive_items(title)").order("created_at",{ascending:false}):{data:[]};return <AdminShell adminEmail={s.admin.email}><h1 className="text-3xl font-black uppercase">Reivindicações</h1><p className="mt-2">Dados desta página são privados e exclusivos da moderação.</p><div className="mt-6 grid gap-3">{data?.map((x:any)=><article key={x.id} className="border-2 border-comun-black bg-white p-4"><h2 className="font-black uppercase">{x.comun_archive_items?.title}</h2><p>{x.claimant_name} · {x.relationship} · {x.status}</p><p>Contato privado: {x.claimant_contact_private}</p><p>Referência privada: {x.verification_reference_private||"não informada"}</p><form action={reviewClaim} className="mt-3 flex flex-wrap gap-2"><input type="hidden" name="id" value={x.id}/><select name="status"><option value="information_requested">Pedir informação</option><option value="verified">Verificar</option><option value="rejected">Rejeitar</option><option value="archived">Arquivar</option></select><input name="moderator_notes" placeholder="Nota interna"/><button className="border-2 border-comun-black bg-comun-yellow p-2 font-black">Registrar</button></form></article>)}</div></AdminShell>}
