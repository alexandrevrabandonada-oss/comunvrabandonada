"use client";
import { useState } from "react";
export function ArchiveUploader({itemId}:{itemId:string}){const [message,setMessage]=useState("");async function upload(formData:FormData){const file=formData.get("file") as File,role=String(formData.get("role"));if(!file?.size)return;setMessage("Preparando upload…");const response=await fetch("/api/comun/admin/archive/upload-url",{method:"POST",headers:{"content-type":"application/json"},body:JSON.stringify({archiveItemId:itemId,filename:file.name,mimeType:file.type,sizeBytes:file.size,role})});const result=await response.json();if(!response.ok){setMessage(result.error??"Falha.");return;}const sent=await fetch(result.uploadUrl,{method:"PUT",headers:{"content-type":file.type},body:file});if(!sent.ok){setMessage("Falha ao enviar para o R2.");return;}setMessage("Upload concluído. Recarregue para revisar o asset.");window.location.reload();}return <form action={upload} className="grid gap-3 border-2 border-comun-black bg-white p-4 md:grid-cols-[1fr_1fr_auto]"><label className="grid gap-1 text-sm font-black uppercase">Papel<select name="role" className="min-h-11 border-2 border-comun-black px-2"><option value="original">Original privado</option><option value="public_version">Versão pública</option><option value="cover">Capa pública</option></select></label><label className="grid gap-1 text-sm font-black uppercase">Arquivo<input name="file" type="file" accept="image/jpeg,image/png,image/webp,application/pdf" required className="min-h-11 border-2 border-comun-black p-2"/></label><button className="min-h-11 self-end border-2 border-comun-black bg-comun-yellow px-4 font-black uppercase">Enviar ao R2</button>{message?<p className="text-sm font-bold md:col-span-3">{message}</p>:null}</form>}
