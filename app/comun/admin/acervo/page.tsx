import Link from "next/link";import {AdminShell} from "@/components/admin-shell";import {requireComunAdmin} from "@/lib/admin-auth";import {listAdminArchiveItems} from "@/lib/archive";export const dynamic="force-dynamic";export default async function AdminArchive(){const session=await requireComunAdmin();const items=await listAdminArchiveItems();return <AdminShell adminEmail={session.admin.email}><div className="flex flex-wrap items-center justify-between gap-3"><div><h1 className="text-3xl font-black uppercase">Acervo</h1><p className="mt-2 text-sm">Originais privados, versões públicas e workflow editorial.</p></div><div className="flex flex-wrap gap-2"><Link href="/comun/admin/acervo/historias-orais" className="border-2 border-comun-black px-3 py-2 font-black uppercase">Histórias orais</Link><Link href="/comun/admin/acervo/colecoes" className="border-2 border-comun-black px-3 py-2 font-black uppercase">Coleções</Link><Link href="/comun/admin/acervo/novo" className="border-2 border-comun-black bg-comun-yellow px-3 py-2 font-black uppercase">Novo item</Link></div></div><div className="mt-6 grid gap-3">{items.map(i=><Link href={`/comun/admin/acervo/${i.id}`} key={i.id} className="grid gap-2 border-2 border-comun-black bg-white p-4 md:grid-cols-[1fr_auto_auto]"><div><h2 className="font-black uppercase">{i.title}</h2><p className="text-xs text-comun-asphalt/60">{i.slug} · {i.item_type}</p></div><span className="font-black uppercase">{i.rights_status}</span><span className="font-black uppercase">{i.status}</span></Link>)}</div>{!items.length?<p className="mt-6 border-2 border-comun-black bg-white p-4">Nenhum item cadastrado.</p>:null}</AdminShell>}
