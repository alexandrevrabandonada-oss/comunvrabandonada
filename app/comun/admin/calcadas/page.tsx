import {AdminShell} from "@/components/admin-shell";
import {requireComunAdmin} from "@/lib/admin-auth";
import {createServiceSupabaseClient} from "@/lib/supabase/server";
import {moderateSidewalkObservation,moderateSidewalkRecord} from "./actions";
export const dynamic="force-dynamic";
export default async function Page(){const session=await requireComunAdmin({roles:["admin","editor"]}),db=createServiceSupabaseClient();if(!db)throw new Error("Banco local indisponível.");const[records,observations]=await Promise.all([db.from("comun_sidewalk_records").select("id,name,condition,categories,created_at,approximate_location,neighborhood,private_geometry_geojson,status").in("status",["under_review","pending"]).order("created_at"),db.from("comun_sidewalk_observations").select("id,record_id,observation_type,note_private,created_at").eq("status","pending").order("created_at")]);return <AdminShell adminEmail={session.admin.email}><h1 className="text-3xl font-black uppercase">Fila das calçadas</h1><p className="mt-2">Área interna: geometrias, fotos e notas exigem decisão humana. Itens mais antigos aparecem primeiro.</p><Queue title="Registros">{(records.data??[]).map((item:any)=><article className="border-2 bg-white p-5" key={item.id}><p className="text-xs font-black uppercase">Recebido em {new Date(item.created_at).toLocaleDateString("pt-BR")} · {item.condition} · {item.status}</p><h2 className="font-black uppercase">{item.name}</h2><p><strong>Problemas:</strong> {item.categories?.join(" · ")||"não classificados"}</p><p><strong>Referência:</strong> {item.approximate_location||item.neighborhood||"protegida"}</p><p><strong>Risco:</strong> revisar rostos, menores, placas, números e local sensível.</p><details><summary className="font-black">Ver geometria privada</summary><pre className="overflow-auto border-2 p-2 text-xs">{JSON.stringify(item.private_geometry_geojson)}</pre></details><form action={moderateSidewalkRecord} className="mt-3 flex flex-wrap gap-2"><input type="hidden" name="record_id" value={item.id}/>{[["approve_approximate","Aprovar com local aproximado"],["publish_without_image","Publicar sem imagem"],["needs_information","Solicitar complemento"],["reject","Rejeitar"],["withdraw","Retirar"]].map(([value,label])=><button key={value} name="decision" value={value} className="btn">{label}</button>)}</form></article>)}</Queue><Queue title="Observações próximas">{(observations.data??[]).map((item:any)=><article className="border-2 bg-white p-5" key={item.id}><p className="text-xs font-black uppercase">{item.observation_type} · {new Date(item.created_at).toLocaleDateString("pt-BR")}</p><p className="mt-2"><strong>Nota privada:</strong> {item.note_private||"sem complemento"}</p><form action={moderateSidewalkObservation} className="mt-3 flex gap-2"><input type="hidden" name="observation_id" value={item.id}/><button name="status" value="approved" className="btn">Aprovar</button><button name="status" value="rejected" className="btn">Rejeitar</button></form></article>)}</Queue></AdminShell>}
function Queue({title,children}:{title:string;children:React.ReactNode}){return <section className="mt-7 grid gap-4"><h2 className="text-2xl font-black uppercase">{title}</h2>{children}</section>}
