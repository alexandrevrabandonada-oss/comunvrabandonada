import { ComunShell, Section } from "@/components/comun-shell"; import { CommunitySignupForm } from "@/components/community-auth-form";
export default async function CriarConta({ searchParams }: { searchParams: Promise<{ returnTo?: string }> }) { const { returnTo } = await searchParams; return <ComunShell><Section><h1 className="text-3xl font-black uppercase text-comun-paper">Criar conta comunitária</h1><p className="mt-3 max-w-xl text-comun-paper/75">Use um nome que pode ser pseudônimo. Seu e-mail não é público.</p><div className="mt-6 max-w-md bg-comun-paper p-5"><CommunitySignupForm returnTo={returnTo}/></div></Section></ComunShell> }
