import { saveCommunityProfileAction } from "@/app/actions"; import { ComunShell, Section } from "@/components/comun-shell"; import { requireCommunitySession } from "@/lib/community-auth";
export default async function Onboarding(){const { profile }=await requireCommunitySession("/comun/onboarding");return <ComunShell><Section><h1 className="text-3xl font-black uppercase text-comun-paper">Seu perfil comunitário</h1><p className="mt-3 text-comun-paper/75">Você pode usar pseudônimo. Os campos opcionais não alimentam recomendações automáticas.</p><form action={saveCommunityProfileAction} className="mt-6 grid max-w-xl gap-4 bg-comun-paper p-5"><label>Nome de exibição<input name="display_name" defaultValue={profile?.display_name||""} required className="mt-1 w-full border-2 border-comun-black p-2"/></label><label>Bio pública opcional<textarea name="public_bio" className="mt-1 w-full border-2 border-comun-black p-2"/></label><label>Visibilidade<select name="profile_visibility" defaultValue={profile?.profile_visibility||"private"} className="mt-1 w-full border-2 border-comun-black p-2"><option value="private">Privada</option><option value="pauta_members">Membros de pautas</option><option value="public">Pública</option></select></label><button className="min-h-11 bg-comun-yellow font-black uppercase">Concluir</button></form></Section></ComunShell>}
