import Link from "next/link"; import {notFound} from "next/navigation";
import {MiniAppExperienceShell,MiniAppPage} from "@/components/sidewalk-miniapp-shell"; import {getSidewalkMiniapp} from "@/lib/sidewalk-miniapp";
export const dynamic="force-dynamic";
export default async function Page(){const data=await getSidewalkMiniapp();if(!data)notFound();return <MiniAppExperienceShell active="resultados" count={data.records.length} community={data.pauta.community} coverage={data.config?.coverage_status} status={data.pauta.public_status}><MiniAppPage title="Resultados e memória" intro="Mudanças verificadas ficam separadas de respostas institucionais, promessas e atividades realizadas."><div className="grid gap-7"><section><h3 className="text-xl font-black">O que mudou</h3><div className="mt-3 grid gap-4 lg:grid-cols-2">{data.results.map((x:any)=><article key={x.id} className="border-2 border-comun-black bg-white p-5"><p className="text-xs font-bold">{x.result_type} · {x.verification_status}</p><h4 className="mt-2 text-xl font-black">{x.title}</h4><p className="mt-3">{x.public_summary}</p><dl className="mt-4 grid gap-2 text-sm"><Row label="Evidência" value="Registro público revisado"/><Row label="Limitações" value="Atribuição causal não presumida"/><Row label="Continuidade" value={data.pauta.next_step??"Acompanhamento comunitário"}/><Row label="Registros afetados" value="Consulte os vínculos publicados"/></dl></article>)}{!data.results.length?<p className="border-2 border-dashed bg-white p-5">Nenhum resultado comprovado foi publicado. Respostas e promessas não aparecem aqui como mudança.</p>:null}</div></section><section><h3 className="text-xl font-black">Memória relacionada</h3><div className="mt-3 grid gap-3">{data.memories.map((x:any)=><Link href={`/comun/pautas/calcadas-em-circulacao/memoria/${x.slug}`} key={x.id} className="border-2 border-comun-black bg-white p-5 hover:bg-comun-yellow"><span className="text-xs font-bold">MEMÓRIA DO PROCESSO</span><strong className="mt-2 block text-xl">{x.title}</strong><span className="mt-2 block">{x.public_summary}</span></Link>)}{!data.memories.length?<p className="border-2 border-dashed bg-white p-5">A memória será publicada depois de revisão editorial.</p>:null}</div></section></div></MiniAppPage></MiniAppExperienceShell>}
function Row({label,value}:{label:string;value:string}){return <div><dt className="font-bold">{label}</dt><dd className="text-comun-black/70">{value}</dd></div>}
