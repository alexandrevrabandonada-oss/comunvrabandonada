import {NextResponse} from "next/server";import {pressurePackageMarkdown} from "@/lib/sidewalk-pressure-package";import {getPublicPressurePackage} from "@/lib/sidewalk-pressure-server";export async function GET(request:Request,{params}:{params:Promise<{id:string}>}){const p=await getPublicPressurePackage((await params).id);if(!p)return new NextResponse("Não encontrado",{status:404});const format=new URL(request.url).searchParams.get("formato");if(format==="md")return new NextResponse(pressurePackageMarkdown(p),{headers:{"content-type":"text/markdown; charset=utf-8","content-disposition":"attachment; filename=pressao-popular.md"}});return NextResponse.json(p,{headers:{"content-disposition":"attachment; filename=pressao-popular.v1.json"}})}
