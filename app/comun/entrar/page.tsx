import Link from "next/link"; import { ComunShell, Section } from "@/components/comun-shell"; import { CommunityLoginForm } from "@/components/community-auth-form";
export default async function Entrar({ searchParams }: { searchParams: Promise<{ returnTo?: string }> }) { const { returnTo } = await searchParams; return <ComunShell><Section><h1 className="text-3xl font-black uppercase text-comun-paper">Entrar no COMUN</h1><div className="mt-6 max-w-md bg-comun-paper p-5"><CommunityLoginForm returnTo={returnTo}/><p className="mt-4 text-sm">Ainda não tem conta? <Link className="font-bold underline" href={`/comun/criar-conta${returnTo ? `?returnTo=${encodeURIComponent(returnTo)}` : ""}`}>Criar conta</Link></p></div></Section></ComunShell> }
